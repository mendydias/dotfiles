return {
  "sainnhe/sonokai",
  config = function()
    vim.g.sonokai_enable_italic = true
    -- The available strings are: 'default'`, `'atlantis'`, `'andromeda'`, `'shusia'`, `'maia'`, `'espresso'`
    vim.g.sonokai_style = "andromeda"
  end,
}
